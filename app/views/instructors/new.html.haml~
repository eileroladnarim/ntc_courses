.row
    %h1 Add Instructor
    =form_for @instructor do |f|
        = f.label :name
        = f.text_field :name
        = f.label :birth_date
        = f.text_field  :birth_date , { :class => 'datepicker' }
        = f.submit "Create", { :class => 'button' }
 
