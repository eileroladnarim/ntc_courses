.row
    %h1 Add Course
    =form_for @course do |f|
        = f.label :name
        = f.text_field :name
        = f.label :description
        = f.text_area  :description 
        = f.submit "Create", { :class => 'button' }
    = link_to "Produce excel File",{ :controller => :courses, :action => :produceexcel}
